select count(PID) from patient;

select DName,count(PID) from appointment,doctor where doctor.did=appointment.did  group by DID;

select avg(fee) from appointment;

select PName from patient where PID in (select distinct PID from appointment);

2.1-

select DName from doctor where DID not in (select distinct DID from appointment);

select pname from patient where pid in
	(select pid from appointment where fee in 
		(select max(fee) from appointment));

2.4-

2.5- 

select specialisation, avg(fee) from doctor group by specialisation;


